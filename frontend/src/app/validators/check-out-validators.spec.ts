import { CheckoutValidators } from './check-out-validators';

describe('CheckOutValidators', () => {
  it('should create an instance', () => {
    expect(new CheckoutValidators()).toBeTruthy();
  });
});
